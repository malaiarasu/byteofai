source "https://rubygems.org"

# Matches the Jekyll + plugin versions GitHub Pages uses in production,
# so local `bundle exec jekyll serve` previews behave the same as the
# live site. Only needed if you want to preview locally — GitHub Pages
# itself does not require this file.
gem "github-pages", group: :jekyll_plugins
